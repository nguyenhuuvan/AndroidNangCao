<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.assmob201.poly.assignment">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.ManHinhChoActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.HomeActivity"
            android:label="Menu"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.NewsActivity"
            android:label="@string/title_activity_news"
            android:parentActivityName=".ui.HomeActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.assmob201.poly.assignment.ui.HomeActivity" />
        </activity>
        <activity
            android:name=".ui.WebViewsActivity"
            android:label="@string/title_activity_web_views"
            android:parentActivityName=".ui.NewsActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.assmob201.poly.assignment.ui.NewsActivity" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.MapsActivity"
            android:label="@string/title_activity_maps">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.assmob201.poly.assignment.ui.HomeActivity" />
        </activity>
        <activity
            android:name=".ui.CourseActivity"
            android:label="@string/title_activity_course"
            android:parentActivityName=".ui.HomeActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.assmob201.poly.assignment.ui.HomeActivity" />
        </activity>
        <activity
            android:name=".ui.IntroCourseActivity"
            android:label="@string/title_activity_intro_course"
            android:parentActivityName=".ui.CourseActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.assmob201.poly.assignment.ui.CourseActivity" />
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <provider android:authorities="com.facebook.app.FacebookContentProvider251714628822983"
            android:name="com.facebook.FacebookContentProvider"
            android:exported="true"/>

        <activity
            android:name=".FacebookActivity"
            android:label="@string/title_activity_facebook"
            android:parentActivityName=".ui.HomeActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.assmob201.poly.assignment.ui.HomeActivity" />
        </activity>
        <activity android:name=".ChucnangActivity"></activity>
    </application>

</manifest>